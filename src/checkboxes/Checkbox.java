package checkboxes;

/**
 * A interface Checkbox define o comportamento de uma caixa de seleção.
 * Assim como a interface Button, qualquer classe que implementar essa interface
 * deve fornecer uma implementação do método 'paint'.
 */
public interface Checkbox {
    void paint();
}

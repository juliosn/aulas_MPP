package buttons;

/**
 * A interface Button define o comportamento de um botão.
 * Qualquer classe que implementar essa interface deve fornecer uma implementação
 * do método 'paint', que será responsável pela renderização do botão.
 */
public interface Button {
    void paint();
}
